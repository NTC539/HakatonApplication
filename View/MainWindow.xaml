<Window x:Class="HakatonApplication.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Хакатоны" Height="800" Width="1200">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Каталог хакатонов" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Width="300" Height="30" Margin="0,0,10,0"/>
            <Button Content="Поиск" Command="{Binding SearchCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Content="Сбросить" Command="{Binding LoadHakatonsCommand}" Width="80" Height="30"/>
            <TextBlock Text="{Binding Hakatons.Count, StringFormat='Найдено: {0} хакатонов'}"
                       VerticalAlignment="Center" Margin="15,0,0,0"/>
        </StackPanel>

        <ProgressBar Grid.Row="2" IsIndeterminate="{Binding IsLoading}"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Height="5" VerticalAlignment="Top"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Hakatons}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8"
                                Margin="0,0,0,15" Padding="15" Background="White">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="22" FontWeight="Bold" Margin="0,0,0,5"/>

                                <WrapPanel Margin="0,0,0,10">
                                    <ItemsControl ItemsSource="{Binding Tags}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#E0E0E0" CornerRadius="12" Padding="8,3" Margin="0,0,8,5">
                                                    <TextBlock Text="{Binding}" FontSize="12"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </WrapPanel>

                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,12"/>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="📅 " />
                                    <TextBlock Text="{Binding StartDate, StringFormat='dd.MM.yyyy'}" />
                                    <TextBlock Text=" – " />
                                    <TextBlock Text="{Binding EndDate, StringFormat='dd.MM.yyyy'}" />
                                </StackPanel>

                                <WrapPanel Margin="0,0,0,10">
                                    <Border Background="#F0F0F0" CornerRadius="8" Padding="8,4" Margin="0,0,10,5">
                                        <TextBlock Text="{Binding TotalTeams, StringFormat='👥 Команд: {0}'}" />
                                    </Border>
                                    <Border Background="#F0F0F0" CornerRadius="8" Padding="8,4" Margin="0,0,10,5">
                                        <TextBlock Text="{Binding TotalParticipants, StringFormat='👤 Участников: {0}'}" />
                                    </Border>
                                    <Border Background="#F0F0F0" CornerRadius="8" Padding="8,4" Margin="0,0,10,5">
                                        <TextBlock Text="{Binding TotalExperts, StringFormat='🧑‍🏫 Экспертов: {0}'}" />
                                    </Border>
                                    <Border Background="#F0F0F0" CornerRadius="8" Padding="8,4" Margin="0,0,10,5">
                                        <TextBlock Text="{Binding TotalPrizeFund, StringFormat='💰 Призовой фонд: {0:N0} ₽'}" />
                                    </Border>
                                </WrapPanel>

                                <StackPanel Margin="0,0,0,10">
                                    <TextBlock Text="Спонсоры:" FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{Binding Sponsors}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" Margin="0,0,15,0" FontSize="12"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <StackPanel>
                                    <TextBlock Text="Эксперты:" FontWeight="SemiBold"/>
                                    <ItemsControl ItemsSource="{Binding Experts}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" Margin="0,0,15,0" FontSize="12"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <Button Content="Подробнее" HorizontalAlignment="Left" Margin="0,15,0,0"
                                        Command="{Binding DataContext.OpenHakatonCommand,
                                                         RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>