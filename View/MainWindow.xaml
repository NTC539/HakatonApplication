<Window x:Class="HakatonApplication.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:converters="clr-namespace:HakatonApplication.Converter"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:HakatonApplication.View"
        xmlns:viewModel="clr-namespace:HakatonApplication.ViewModel"
        Title="Хакатоны" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:MainWindowViewModel}">
            <view:HakatonListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:HakatonDetailViewModel}">
            <view:HakatonDetailView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>