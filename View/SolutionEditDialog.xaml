<Window x:Class="HakatonApplication.View.SolutionEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Решение" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    <StackPanel Margin="10">
        <TextBlock Text="Название"/>
        <TextBox Text="{Binding Name}" Margin="0,5,0,10"/>
        <TextBlock Text="Описание"/>
        <TextBox Text="{Binding Description}" Height="80" TextWrapping="Wrap" Margin="0,5,0,10"/>
        <TextBlock Text="Ссылка на репозиторий / файл"/>
        <TextBox Text="{Binding Source}" Margin="0,5,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="75"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="75" Margin="5,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>